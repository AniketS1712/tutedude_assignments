import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — Isar requires native binaries for testing.
    expect(true, isTrue);
  });
}
